node_modules
dist
dist-web
dist-electron
release

# Vite env files — ALL gitignored.
#
# Each developer / kiosk maintains its own .env.* locally. To onboard,
# copy the relevant section from .env.example and fill in values.
.env
.env.local
.env.development
.env.development.local
.env.production
.env.production.local

# Supabase CLI state — per-developer
supabase/.temp/

# RSA / TLS keys — must NEVER be committed
*.pem
*.key

.DS_Store
*.log
*.tsbuildinfo
